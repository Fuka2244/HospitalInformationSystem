# 医院信息系统（HIS）—— 系统功能与技术路线文档

---

## 一、系统概述

本系统是一个基于 **Spring Boot + Vue 3** 前后端分离架构的医院信息管理系统，核心特色是深度集成了以**通义千问（Qwen）**大语言模型为底座的四大AI智能服务，覆盖智能导诊、药品推荐、费用解释、医疗报告生成全业务场景，旨在为患者提供便捷、透明、智慧化的医疗服务体验。

---

## 二、系统具体功能

### 2.1 患者管理模块

#### 2.1.1 用户注册

| 项目 | 说明 |
|------|------|
| 功能描述 | 新患者通过填写个人信息创建账户 |
| 输入 | 姓名、用户名、密码、确认密码、性别（男/女）、年龄、手机号、地址、身份证号 |
| 输出 | 注册成功/失败提示 |
| 校验规则 | 手机号正则 `1[3-9]\d{9}`；身份证18位；密码≥6位；确认密码一致性；手机号唯一性 |
| 业务逻辑 | 后端自动生成UUID作为账户主键；密码使用BCrypt加密存储 |

#### 2.1.2 用户登录

| 项目 | 说明 |
|------|------|
| 功能描述 | 已注册患者通过手机号+密码登录系统 |
| 输入 | 手机号、密码 |
| 输出 | 登录成功后获取完整用户信息，存入Session |
| 业务逻辑 | BCrypt密码校验；登录成功构建PatientInfoVo（含就诊统计：总次数、最近就诊日期）写入HttpSession |

#### 2.1.3 忘记密码

| 项目 | 说明 |
|------|------|
| 功能描述 | 用户通过手机验证码重置密码 |
| 输入 | 手机号、验证码、新密码、确认密码 |
| 业务逻辑 | Redis存储验证码（5分钟有效）；60秒发送限流；验证码校验通过后BCrypt加密新密码更新 |

#### 2.1.4 个人信息查看与修改

| 项目 | 说明 |
|------|------|
| 查看 | 姓名、性别、年龄、账户ID、手机号、身份证（脱敏）、地址、就诊次数、最近就诊、头像 |
| 修改 | 用户名、手机号、地址（需输入当前密码验证） |
| 身份证查看 | 默认脱敏（前3位+***+后4位），输入密码后可查看完整号码 |
| 头像管理 | 支持上传头像（≤5MB，jpg/png/gif），点击头像区域触发上传，覆盖旧头像 |

#### 2.1.5 电子病历查询

| 项目 | 说明 |
|------|------|
| 列表展示 | 就诊日期、科室、医生、诊断、处方摘要 |
| 详情展示 | 患者/医生/科室信息、就诊日期、主诉、现病史、诊断、治疗方案 + 处方明细（药品名、用法用量、数量、天数） |
| 权限控制 | 患者仅看自己；医生看自己接诊过的患者；管理员/药师看所有 |

#### 2.1.6 就诊历史查询

| 项目 | 说明 |
|------|------|
| 筛选条件 | 科室、医生、日期范围 |
| 展示内容 | 就诊日期、医生、科室、诊断、处方摘要 |
| 分页 | 支持每页10/20/50条 |

---

### 2.2 预约挂号模块

#### 2.2.1 AI智能导诊

| 项目 | 说明 |
|------|------|
| 功能描述 | 患者通过自然语言描述症状，AI智能推荐就诊科室和医生 |
| 交互方式 | 多轮对话式：患者逐步描述症状 → AI追问细节 → 信息充足后自动推荐 |
| 输入 | 患者症状描述文本 |
| 输出 | 推荐科室、推荐医生、推荐日期时段、推荐理由 + 可用排班列表 |
| 对话状态 | AI内部标记 completed 字段：false=继续对话，true=已完成推荐 |
| 后续操作 | 患者可从推荐排班中选择时段，直接确认预约 |

#### 2.2.2 手动创建预约

| 项目 | 说明 |
|------|------|
| 步骤 | ①选择科室和日期 → ②浏览医生列表 → ③查看医生排班 → ④选择时段 → ⑤确认预约 |
| 预约类型 | 医生预约（DOCTOR）、检查预约（EXAMINATION，如CT/MRI） |
| 并发控制 | Redis分布式锁 + MySQL原子SQL递增booked_count，防止超卖 |
| 防重复 | 同一患者同一天同一时段不可重复预约 |

#### 2.2.3 预约管理

| 项目 | 说明 |
|------|------|
| 预约列表 | 展示预约日期、时段、科室、医生、类型、状态，支持多维度筛选 |
| 取消预约 | 仅"已预约"状态可取消，需填写取消原因；取消后释放号源（原子递减booked_count） |
| 改期预约 | 先取消旧预约释放号源，再创建新预约占用新号源，分布式锁保护 |
| 预约状态 | 0=已预约、1=已完成、2=已取消 |

#### 2.2.4 排班查询

| 项目 | 说明 |
|------|------|
| 查询条件 | 科室ID、医生ID、日期（默认查询未来7天） |
| 展示信息 | 医生姓名、科室、排班日期、时段、最大接诊数、已预约数、状态 |

---

### 2.3 费用管理模块

#### 2.3.1 费用查询

| 项目 | 说明 |
|------|------|
| 筛选条件 | 费用类型（挂号/检查/药品/其他）、支付状态（未支付/已支付/已退款）、日期范围 |
| 统计卡片 | 本页费用总额、已支付金额、未支付金额、本页账单数 |
| 费用详情 | 时间、类型、项目名称、金额、状态、说明 |

#### 2.3.2 费用构成可视化

| 项目 | 说明 |
|------|------|
| 图表类型 | SVG环形饼图，中心显示"总计"+金额 |
| 交互 | 悬浮扇区外移7px高亮；点击扇区弹出该类费用详情 |
| 图例 | 挂号（绿色）、检查（橙色）、药品（蓝色）、其他（灰色） |
| 实现 | 纯SVG渲染，无第三方图表库依赖 |

#### 2.3.3 AI费用解释

| 项目 | 说明 |
|------|------|
| 功能描述 | AI分析患者费用明细，提供通俗易懂的费用解读和建议 |
| 交互方式 | 多轮对话：患者可追问费用细节 |
| 单次解释 | 输入问题+日期范围 → AI返回费用总计、费用构成、详细解释、节省建议 |
| 多轮对话 | 保留历史消息上下文，AI可基于费用数据连续回答 |

---

### 2.4 医疗报告模块

#### 2.4.1 AI报告生成（核心功能）

| 项目 | 说明 |
|------|------|
| 功能描述 | 基于病历数据，AI通过思维链（Chain-of-Thought）自动生成医疗分析报告 |
| 生成方式 | SSE流式实时展示 + 非流式同步生成两种模式 |
| 输入 | 选择病历 + 报告类型（检查报告/治疗报告）+ 报告标题 + 检查数据 |
| 输出 | AI摘要、AI诊断、AI治疗方案、AI建议、AI思维链 |
| 流式进度 | 6步实时推送：①数据预处理 → ②分析主诉现病史 → ③结合检查结果 → ④参考历史记录 → ⑤综合分析诊断 → ⑥提出治疗方案 |
| 思维链展示 | 左侧步骤指示器（当前步骤蓝色高亮、已完成绿色打勾）+ 右侧详细分析文本 |
| 取消支持 | 前端AbortController可中断流式生成 |

#### 2.4.2 报告管理

| 项目 | 说明 |
|------|------|
| 报告列表 | 展示报告标题、类型、状态（草稿/已确认/已作废）、生成时间 |
| 报告详情 | 基本信息 + AI思维链5步分析 + AI分析结果（摘要/诊断/治疗方案/康复建议） |
| 确认报告 | 将草稿状态改为已确认 |
| 导出PDF | 使用iTextPDF生成PDF，支持中文字体（宋体），下载到本地 |
| 免责声明 | 所有AI报告页面均显示"AI报告仅供参考，请以医生诊断为准" |

---

### 2.5 药品管理模块

#### 2.5.1 药品查询

| 项目 | 说明 |
|------|------|
| 搜索条件 | 关键词（匹配名称/通用名/功效）+ 分类（中成药/化学药品与生物制品） |
| 列表展示 | 药品名称、通用名、分类、规格、功效、用法用量、价格 |
| 详情展示 | 完整信息：名称、通用名、分类、规格、厂家、价格、库存、成分、功效、用法用量、副作用、禁忌 |

#### 2.5.2 AI药品推荐

| 项目 | 说明 |
|------|------|
| 功能描述 | 基于症状描述，AI推荐合适的药品 |
| 交互方式 | 多轮对话式 |
| 推荐结果 | 药品名、推荐理由、用法用量、注意事项 |
| 技术实现 | RAG（检索增强生成）—— 先向量检索Top-10候选药品，再LLM精排推荐3种 |
| 免责声明 | "AI推荐仅供参考，请遵医嘱用药" |

---

### 2.6 科室信息模块

| 项目 | 说明 |
|------|------|
| 科室列表 | 展示所有科室的图标、名称、描述，点击切换详情 |
| 科室详情 | 科室名称、位置标签、科室描述 |
| 医生列表 | 展示科室下所有医生的姓名、职称、性别、年龄、擅长领域 |
| 全部医生 | 支持查看全院医生列表 |

---

### 2.7 文件管理模块

| 项目 | 说明 |
|------|------|
| 头像上传 | 支持jpg/png/gif格式，≤5MB，以账户ID命名，自动删除旧头像 |
| 头像展示 | 导航栏用户下拉显示头像；个人信息页展示头像，悬浮显示"更换头像"遮罩 |

---

## 三、技术路线

### 3.1 整体技术架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端技术栈                                │
│  Vue 3 + TypeScript + Element Plus + Pinia + Vue Router + Vite  │
├─────────────────────────────────────────────────────────────────┤
│                        通信协议                                  │
│  HTTP/RESTful API + SSE(Server-Sent Events) + Session会话管理    │
├─────────────────────────────────────────────────────────────────┤
│                        后端技术栈                                │
│  Spring Boot + MyBatis-Plus + Redis + LangChain4j + iTextPDF    │
├─────────────────────────────────────────────────────────────────┤
│                        AI技术栈                                  │
│  通义千问 Qwen-Plus (Chat) + Text-Embedding-V2 (RAG)            │
│  DashScope API (OpenAI兼容协议)                                  │
├─────────────────────────────────────────────────────────────────┤
│                        数据存储                                  │
│  MySQL (业务数据) + Redis (缓存/锁) + 文件系统 (上传文件/向量缓存) │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 前端技术路线

#### 3.2.1 框架与语言

| 技术 | 版本 | 选型理由 |
|------|------|---------|
| Vue 3 | 3.x | Composition API + `<script setup>` 语法，逻辑复用性强，TypeScript支持好 |
| TypeScript | 5.x | 静态类型检查，接口定义清晰，约30个接口/类型定义，减少运行时错误 |
| Vite | 5.x | 开发服务器秒启动，HMR快速，构建性能优于Webpack |

#### 3.2.2 UI与交互

| 技术 | 选型理由 |
|------|---------|
| Element Plus | 企业级Vue 3组件库，中文支持好，表单/表格/对话框组件丰富 |
| 响应式设计 | CSS媒体查询适配，≤1100px隐藏品牌文字，≤768px菜单仅图标 |
| 全局样式系统 | global.css定义 `.page`/`.fill-row`/`.fill-col`/`.fill-card` 弹性布局体系 |

#### 3.2.3 状态与路由

| 技术 | 选型理由 |
|------|---------|
| Pinia | Vue 3官方推荐状态管理，轻量，TypeScript支持好，替代Vuex |
| Vue Router 4 | HTML5 History模式，路由守卫统一鉴权，懒加载优化首屏 |
| 路由守卫策略 | 非强制跳转：公开页面直接放行，受保护页面尝试fetchProfile，失败不强制跳转登录页 |

#### 3.2.4 网络通信

| 技术 | 选型理由 |
|------|---------|
| Axios | HTTP客户端，拦截器统一处理401跳转和错误提示 |
| Fetch API | SSE流式报告专用：原生ReadableStream解析SSE事件，支持AbortController取消 |

#### 3.2.5 前端关键技术实现

**1) SSE流式报告生成**

```
用户点击"生成"
  → fetch(url, {method:'POST', body, signal:abortController.signal})
  → ReadableStream逐块读取
  → 解析SSE文本: "event:step\ndata:{...}\n\n"
  → 按事件类型分发:
      step        → 更新进度条和步骤指示器
      thoughtchain → 显示AI思维链文本
      complete    → 显示完整报告，关闭生成对话框
      error       → 显示错误提示
      finish      → 结束读取循环
  → 用户点击"取消" → abortController.abort() → 中断fetch
```

**2) AI多轮对话UI**

```
统一对话组件模式:
┌────────────────────────────┐
│ 消息列表 (scrollable)       │
│  🤖 AI消息 (蓝色渐变气泡)   │
│  👤 用户消息 (紫色渐变)     │
│  ⏳ AI输入动画 (三点跳动)   │
│  📋 推荐结果卡片            │
└────────────────────────────┘
│ 💬 [输入框]       [发送]    │
└────────────────────────────┘

对话状态管理:
- messages: ChatMessageDto[] (role+content)
- loading: boolean (AI思考中)
- recommendation: 推荐结果对象 (完成时显示)
```

**3) SVG环形饼图**

```
纯SVG实现，无第三方图表库:
- 计算各扇区角度 → SVG arc path绘制
- 悬浮交互: mouseenter→扇区外移7px，mouseleave→归位
- 点击交互: emit('select', item) → 父组件弹出详情
- 中心文本: "总计" + 格式化金额
- 全圆处理: 当某项>99.9%时特殊绘制完整圆
```

---

### 3.3 后端技术路线

#### 3.3.1 框架与ORM

| 技术 | 选型理由 |
|------|---------|
| Spring Boot 3.x | 自动配置、内嵌Tomcat、生态完善 |
| MyBatis-Plus | 通用CRUD无需写SQL；LambdaQuery类型安全；分页插件开箱即用 |
| MySQL 8.x | 关系型数据存储，支持事务，医院业务数据结构化强 |
| Redis | 验证码存储（5分钟TTL）；分布式锁（预约防超卖） |

#### 3.3.2 会话与鉴权

| 技术 | 选型理由 |
|------|---------|
| HttpSession | 简单可靠，适合单体应用；Session超时30分钟 |
| LoginInterceptor | 拦截所有路径，排除公开API；检查Session中phone属性 |
| 4级角色权限 | patient（患者）、doctor（医生）、pharmacist（药师）、admin（管理员） |
| 身份证脱敏 | 默认前3+***+后4，密码验证后返回完整号码 |

#### 3.3.3 并发控制

**预约防超卖——双重保障机制：**

```
方式一：Redis分布式锁
┌──────────────────────────────────────────┐
│ String lockKey = "appointment:lock:"      │
│     + doctorId + ":" + date + ":" + slot; │
│ RLock lock = redisson.getLock(lockKey);   │
│ lock.lock();                              │
│ try {                                      │
│     // 检查booked_count < max_patients     │
│     // 创建预约 + 递增booked_count         │
│ } finally {                                │
│     lock.unlock();                         │
│ }                                          │
└──────────────────────────────────────────┘

方式二：MySQL原子SQL（兜底保障）
┌──────────────────────────────────────────┐
│ UPDATE doctor_schedule                     │
│ SET booked_count = booked_count + 1       │
│ WHERE doctor_id=? AND schedule_date=?     │
│   AND time_slot=?                         │
│   AND booked_count < max_patients         │
│   AND status = 1                          │
│ -- 影响行数=0 表示已满，预约失败           │
└──────────────────────────────────────────┘
```

取消预约同理：分布式锁 + 原子递减booked_count。

#### 3.3.4 文件与PDF

| 技术 | 选型理由 |
|------|---------|
| MultipartFile | Spring标准文件上传，支持类型/大小校验 |
| 本地文件系统 | 上传目录 `${user.dir}/uploads`，WebConfig静态资源映射访问 |
| iTextPDF | 报告PDF导出，支持中文字体（宋体BaseFont），自动布局 |

---

### 3.4 AI技术路线

#### 3.4.1 AI基础设施

| 组件 | 配置 | 说明 |
|------|------|------|
| ChatLanguageModel | qwen-plus, temperature=0.7, maxTokens=2048 | 通用对话与推理 |
| EmbeddingModel | text-embedding-v2 | RAG向量嵌入 |
| EmbeddingStore | InMemoryEmbeddingStore + 磁盘缓存 | 药品向量存储 |
| API协议 | DashScope OpenAI兼容 | 统一调用接口 |

**AiConfig配置类核心Bean：**

```
@Bean ChatLanguageModel  → OpenAiChatModel (qwen-plus)
@Bean EmbeddingModel     → OpenAiEmbeddingModel (text-embedding-v2)
@Bean EmbeddingStore     → InMemoryEmbeddingStore (优先从磁盘缓存加载)
```

#### 3.4.2 AI智能导诊 (AiAppointmentService)

**技术路线：Prompt上下文注入**

```
① 数据准备
   DB → 科室列表 → 格式化文本
   DB → 医生列表 → 格式化文本(含科室/职称/专长)
   DB → 排班列表 → 格式化文本

② Prompt构建
   System Prompt: 你是医院智能导诊助手...
   + 科室信息上下文
   + 医生信息上下文
   + 排班信息上下文（含排班推荐时）

③ LLM推理
   Qwen-Plus → 输出JSON格式推荐结果
   {department, doctor, reason, timeSlot, ...}

④ 结果解析
   JSON → AppointmentRecommendation 对象
   排班推荐时: 反查科室/医生ID → 校验排班可用性

⑤ 多轮对话
   用户消息 + 历史消息 → LLM判断是否信息充足
   不足: 返回追问 (completed=false)
   充足: 自动触发推荐 (completed=true + recommendation)
```

**为什么不选RAG？** 科室/医生数据量小（数十条），直接全量注入Prompt比向量检索更精准、更可控。

#### 3.4.3 AI药品推荐 (AiMedicineService)

**技术路线：RAG（检索增强生成）**

```
━━ 离线索引构建 (CommandLineRunner启动时) ━━

① 加载药品数据
   MedicineMapper.selectList() → 全量药品

② 向量化
   Medicine → TextSegment(名称+通用名+功效+分类+规格)
   → EmbeddingModel.embed() → Embedding向量
   → 批量处理(每批25条, DashScope限制)
   → InMemoryEmbeddingStore.add()

③ 磁盘缓存
   ObjectMapper.writeValue() → medicine-embedding-store.json
   下次启动: 优先从缓存文件加载, 跳过重新构建

━━ 在线查询 ━━

④ 用户输入向量化
   症状文本 → EmbeddingModel.embed() → queryEmbedding

⑤ 向量检索
   EmbeddingSearchRequest(queryEmbedding, maxResults=10, minScore=0.5)
   → 获取Top-10候选药品(含相似度分数)

⑥ LLM精排
   Prompt: 候选药品上下文(含相似度) + 用户症状
   → Qwen-Plus从10个候选中精选3个推荐
   → 输出: [{medicineName, reason, dosage, precautions}]

⑦ 多轮对话
   用户追问 + 历史 + 重新检索(可选) → LLM继续推荐
```

**为什么选RAG？** 药品数据量大（数百条），全量注入Prompt不现实；向量检索可快速缩小范围，LLM精排确保推荐质量。

#### 3.4.4 AI费用解释 (AiBillingService)

**技术路线：Prompt上下文注入**

```
① 数据准备
   DB → 按日期范围查询患者费用列表
   → 格式化为结构化文本(项目/金额/类型/状态)

② Prompt构建
   System Prompt: 你是医疗费用解释助手...
   + 费用明细上下文
   + 用户问题

③ LLM推理
   → BillingExplanation {
       totalAmount: 总金额,
       breakdown: 费用构成分析,
       explanation: 详细解释(通俗语言),
       suggestion: 节省建议
     }

④ 多轮对话
   保留历史 + 费用数据 → 用户可追问具体项目
```

#### 3.4.5 AI报告生成 (AiReportService)

**技术路线：CoT思维链（Chain-of-Thought）**

```
① 数据准备
   MedicalRecord → 主诉/现病史/诊断/治疗方案
   + 检查数据(examinationData)
   + 历史上下文(最近3条病历摘要)

② CoT思维链5步分析
   Step 1: 分析主诉和现病史
         → 识别关键症状、病程、严重程度
   Step 2: 结合检查结果分析
         → 解读检查数据、识别异常指标
   Step 3: 参考历史记录
         → 对比既往病史、发现变化趋势
   Step 4: 综合分析诊断
         → 整合所有信息、给出诊断结论
   Step 5: 提出治疗方案和建议
         → 个性化治疗建议、康复指导

③ 输出
   ReportGenerationResult {
     aiSummary: 病情摘要,
     aiDiagnosis: 诊断结论,
     aiTreatment: 治疗方案,
     aiRecommendation: 康复建议,
     aiThoughtChain: 完整思维链文本
   }
```

**SSE流式报告生成（MedicalReportController）：**

```
Controller层:
  ① 获取病历 + 患者历史
  ② 创建SseEmitter (超时5分钟)
  ③ 异步线程执行:
      for (step 1~6):
          推送 step 事件 {step, title, message, progress}
          执行该步骤的AI分析
      推送 thoughtchain 事件 {aiThoughtChain}
      推送 complete 事件 {完整MedicalReport}
      推送 finish 事件
  ④ 返回 SseEmitter

前端:
  fetch() → ReadableStream → 解析SSE事件 → 实时更新UI
```

---

### 3.5 数据存储技术路线

#### 3.5.1 MySQL

| 用途 | 说明 |
|------|------|
| 业务数据 | 患者信息、科室、医生、排班、预约、病历、报告、药品、费用、处方等13张表 |
| 分页查询 | MyBatis-Plus分页插件，基于MySQL LIMIT |
| 原子操作 | 排班预约数递增/递减，WHERE条件保证数据一致性 |

#### 3.5.2 Redis

| 用途 | Key模式 | TTL | 说明 |
|------|---------|-----|------|
| 验证码 | `verification:code:{phone}` | 5分钟 | 存储手机验证码 |
| 验证码限流 | `verification:limit:{phone}` | 60秒 | 防止频繁发送 |
| 分布式锁 | `appointment:lock:{doctorId}:{date}:{slot}` | 即时释放 | 预约/取消防并发 |

#### 3.5.3 文件系统

| 用途 | 路径 | 说明 |
|------|------|------|
| 头像上传 | `${user.dir}/uploads/` | 以账户ID命名，覆盖旧文件 |
| PDF报告 | 生成后供下载 | iTextPDF即时生成，不持久化 |
| 向量缓存 | `medicine-embedding-store.json` | 药品嵌入向量序列化缓存 |

---

### 3.6 安全技术路线

| 安全措施 | 实现方式 |
|---------|---------|
| 密码加密 | BCrypt哈希，不可逆 |
| 会话管理 | HttpSession + 30分钟超时 |
| 登录拦截 | LoginInterceptor检查Session中phone属性 |
| 身份证脱敏 | 默认部分隐藏，密码验证后可见 |
| 文件上传校验 | 类型白名单(jpg/png/gif) + 大小限制(5MB) |
| SQL注入防护 | MyBatis-Plus参数化查询 |
| 前端401处理 | Axios拦截器自动跳转登录页 |
| 全局异常处理 | GlobalExceptionHandler统一返回Result.error() |

---

### 3.7 四大AI服务技术对比

| 维度 | AI智能导诊 | AI药品推荐 | AI费用解释 | AI报告生成 |
|------|-----------|-----------|-----------|-----------|
| **AI策略** | Prompt注入 | RAG向量检索+LLM精排 | Prompt注入 | CoT思维链 |
| **数据来源** | 科室/医生/排班DB | 药品DB+向量索引 | 费用DB | 病历DB+历史上下文 |
| **交互模式** | 多轮对话 | 多轮对话 | 多轮对话 | SSE流式6步 |
| **LLM输出** | JSON推荐结果 | 药品推荐列表 | 费用解释文本 | 思维链+结构化报告 |
| **适用原因** | 数据量小，全量注入可控 | 数据量大，需检索缩小范围 | 数据结构化，直接注入 | 需要推理过程可解释 |
| **响应方式** | 同步REST | 同步REST | 同步REST | SSE流式 |
| **缓存机制** | 无 | 磁盘向量缓存 | 无 | 无 |

---

## 四、技术路线选型总结

### 4.1 为什么选择前后端分离？

| 优势 | 说明 |
|------|------|
| 开发效率 | 前后端并行开发，Vue Vite秒级HMR |
| 用户体验 | SPA单页应用，无刷新切换 |
| 可维护性 | 前后端独立部署，接口解耦 |
| 扩展性 | 后端可独立扩展为微服务 |

### 4.2 为什么选择Spring Boot + MyBatis-Plus？

| 优势 | 说明 |
|------|------|
| 快速开发 | 自动配置+通用CRUD，减少80%样板代码 |
| 灵活SQL | 复杂查询可自定义XML，原子SQL保证并发安全 |
| 生态成熟 | 与Redis/MySQL/LangChain4j集成简单 |

### 4.3 为什么选择通义千问（Qwen）？

| 优势 | 说明 |
|------|------|
| 中文能力强 | 面向中文场景优化，医疗术语理解准确 |
| API兼容 | DashScope提供OpenAI兼容协议，LangChain4j无缝对接 |
| 性价比 | qwen-plus模型兼顾效果与成本 |
| 生态支持 | text-embedding-v2向量模型，一站式RAG解决方案 |

### 4.4 为什么AI服务采用不同策略？

| 场景 | 数据特征 | 选型策略 | 理由 |
|------|---------|---------|------|
| 智能导诊 | 科室/医生数十条 | Prompt注入 | 数据量小，全量注入更精准可控 |
| 药品推荐 | 药品数百条 | RAG检索 | 数据量大，向量检索缩小范围+LLM精排 |
| 费用解释 | 患者费用动态查询 | Prompt注入 | 数据结构化，按需注入特定患者费用 |
| 报告生成 | 病历+检查+历史 | CoT思维链 | 医疗诊断需要可解释的推理过程 |
